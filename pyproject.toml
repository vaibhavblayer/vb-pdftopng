[tool.poetry]
name = "vb-pdftopng"
version = "0.1.0"
description = "Its a tiny cli tool for converting pdf into pngs."
authors = ["vaibhavblayer <vaibhavblayer@gmail.com>"]
readme = "README.md"
packages = [{include = "vb_pdftopng"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
